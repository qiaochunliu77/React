import React ,{Component }from 'react'; //按需加载

// react 来自于facebook 继承component就拥有了这个组件
class CommentApp extends Component {
    render(){ //接口
        return(
            <div>
                CommentApp
            </div>
        )
    }
}
export default CommentApp